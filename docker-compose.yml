version: "3.8"

services:
  dsl-reviewer:
    build: .
    ports:
      - "8000:8000"
    environment:
        - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./samples:/app/samples   
    restart: unless-stopped
